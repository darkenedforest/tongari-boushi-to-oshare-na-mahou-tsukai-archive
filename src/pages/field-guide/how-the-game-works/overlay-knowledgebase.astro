---
import FieldGuideLayout from '../../../components/FieldGuideLayout.astro';
const base = import.meta.env.BASE_URL;
---

<FieldGuideLayout
  title="Overlay knowledgebase"
  description="The plain-English shape of the 180 ARM9 overlays — what they are, how they load, and why their edits are delicate. The full per-overlay catalog lives at /field-guide/how-the-game-works/overlays/."
  crumbs={[
    { label: 'Field Guide', href: `${base}/field-guide/` },
    { label: 'How the Game Works', href: `${base}/field-guide/how-the-game-works/` },
    { label: 'Overlay knowledgebase' },
  ]}
>
  <p>
    This page is the "what's an overlay" explainer. The
    <a href={`${base}/field-guide/how-the-game-works/overlays-system/`}>overlay system</a>
    page covers the technical specifics (RAM slots, controller/body
    pattern, persistent engines). The full per-overlay catalog — one page
    per overlay, with descriptive role, notable functions, container
    references, and embedded JP strings — lives at the
    <a href={`${base}/field-guide/how-the-game-works/overlays/`}>per-overlay catalog</a>.
  </p>

  <h2>What is an overlay?</h2>
  <p>
    The DS has very little RAM (4 MB total) and Tongari Boushi is a fairly
    large game. To fit, the engine breaks its code into <strong>overlays</strong>:
    chunks of compiled ARM9 code that are loaded from the cartridge into a
    shared RAM region only when they're needed. When the player moves between
    game modes — exiting a shop, opening the mail box, entering the cooking
    minigame — the engine evicts the current overlay and loads the next one
    into the same address.
  </p>
  <p>
    There are <strong>180 overlays</strong> in this game. A handful are
    resident the whole time the game is running (the 2D rendering engine
    <code>ov137</code> is the big one); the rest swap in and out.
  </p>

  <h2>How an overlay loads</h2>
  <ol>
    <li>The state machine in the main ARM9 binary decides "we're now in mode X."</li>
    <li>It looks up overlay X in the overlay table at ROM offset <code>0x8DE00</code>.</li>
    <li>That table row says: load file <code>overlay/overlay_NNNN.bin</code>, decompress if needed, and copy it to address <code>ram_addr</code>.</li>
    <li>If the destination RAM slot already holds a different overlay, that overlay is overwritten.</li>
    <li>The new overlay's entrypoint runs; it sets up state, registers event handlers, and yields back to the main loop.</li>
  </ol>

  <h2>Compression — BLZ for ARM9, code-LZ for overlays</h2>
  <p>
    The main ARM9 binary at ROM offset <code>0x4000</code> ships
    BLZ-compressed (Nintendo's backward-running LZ variant used for
    executable code). Most overlay files at <code>overlay/overlay_NNNN.bin</code>
    ship with their <code>compressed_flag</code> set, meaning the engine runs
    a code-LZ decompressor on them at load time. The
    <a href={`${base}/field-guide/file-formats/blz-arm9/`}>BLZ &amp; the plaintext-ARM9 trick</a>
    page covers the format details, the in-place backward decompression, the
    NitroSDK module-params layout, and the "flip <code>CompressedStaticEnd</code>"
    trick used to load ARM9 as plaintext.
  </p>
  <p>
    The same idea applies per overlay: flipping the per-overlay
    <code>is_compressed</code> bit in the overlay table to <code>0</code> and
    storing the body uncompressed makes the engine load the overlay as
    plaintext. Both shapes are interchangeable on-disk and the engine
    handles either; a re-LZ-compressed body via
    <code>ndspy.codeCompression</code> yields the closest on-disk match to
    the Konami original.
  </p>

  <h2>NitroSDK validation markers — none present</h2>
  <p>
    Retail DS binaries from some publishers carry the NitroSDK code footer
    markers (<code>0xDEC00621</code> "NitroCodeBE", <code>0x2106AED5</code>
    "NitroFooter") used to validate plaintext ARM9 modules at load time.
    Konami stripped or never emitted them on this ROM —
    <strong>all 180 overlays scan clean for both markers</strong>, and the
    main ARM9 plaintext doesn't carry them either. This is normal for retail
    DS titles where overlays ship BLZ-compressed; the markers wouldn't
    survive decompression intact anyway. The patcher does not need to
    re-emit them.
  </p>

  <h2>Why editing overlays is delicate</h2>
  <p>
    Container files in <code>data/*</code> (dialog strings, item tables,
    magic glyphs) are read at runtime through a content cache that does not
    care about their precise byte layout as long as the file format is
    valid. Overlays are different: an overlay is <em>compiled code</em>. It
    has hard-coded addresses, fixed-stride data tables, and pointers into
    <code>.rodata</code> that were resolved at link time. Any byte-level
    edit to an overlay has to either:
  </p>
  <ul>
    <li><strong>Stay byte-exact in length</strong> — replace 4 bytes with
      exactly 4 bytes, never more — so nothing else in the overlay
      shifts; or</li>
    <li><strong>Recompute every pointer in the overlay</strong> that
      pointed past the edit, which means re-resolving the relocation
      table by hand.</li>
  </ul>
  <p>
    Get either of those wrong and the game crashes the first time the
    overlay is loaded. That is the practical constraint behind any tool
    that emits new overlay bytes — each edit is tracked as a specific
    (file, offset, before-bytes, after-bytes) tuple, and the tool refuses
    to apply if the original bytes don't match the expected shape.
  </p>

  <h2>Per-overlay catalog</h2>
  <div class="fg-note">
    <strong>One descriptive page per overlay.</strong>
    All 180 overlays have been put through a Ghidra-based
    reverse-engineering pass and have their own page with the function
    listing Ghidra found, the container files each overlay references, and
    any Japanese strings baked directly into the overlay binary. Jump to
    the
    <a href={`${base}/field-guide/how-the-game-works/overlays/`}>per-overlay
    catalog</a>; the landing page is searchable and filterable, and each
    row drills into the per-overlay detail page.
  </div>

  <h2>Related</h2>
  <ul>
    <li><a href={`${base}/field-guide/how-the-game-works/overlays-system/`}>The overlay system</a> — RAM slots, controller/body pattern, persistent engines.</li>
    <li><a href={`${base}/field-guide/how-the-game-works/overlays/`}>Per-overlay catalog</a> — one descriptive page per overlay.</li>
    <li><a href={`${base}/field-guide/how-the-game-works/memory-map/`}>Memory map</a> — where overlays land in main RAM.</li>
    <li><a href={`${base}/field-guide/file-formats/blz-arm9/`}>BLZ &amp; the plaintext-ARM9 trick</a> — the executable-code compression used on ARM9 and overlays.</li>
    <li><a href={`${base}/field-guide/names-and-words/arm9-menu-pool/`}>ARM9 menu pool</a> — the 52 menu-string slots embedded in ARM9 and adjacent overlay slots.</li>
  </ul>
</FieldGuideLayout>
